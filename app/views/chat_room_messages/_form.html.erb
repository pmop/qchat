<%= form_with(model: chat_room_message) do |form| %>
  <% if chat_room_message.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(chat_room_message.errors.count, "error") %> prohibited this chat_room_message from being saved:</h2>

      <ul>
        <% chat_room_message.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :chat_room_id, style: "display: block" %>
    <%= form.text_field :chat_room_id %>
  </div>

  <div>
    <%= form.label :user_name, style: "display: block" %>
    <%= form.text_field :user_name %>
  </div>

  <div>
    <%= form.label :content, style: "display: block" %>
    <%= form.text_field :content %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
